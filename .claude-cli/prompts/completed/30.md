Sua tarefa é revisar e melhorar toda a documentação `dartdoc` (`///`) do código deste pacote Flutter presente em "./lib/".

**Instruções:**

- Melhore a clareza, complete a informação e adicione exemplos de código (` ```dart ... ``` `) onde for útil.
- Documente apenas APIs públicas (classes, métodos, propriedades não iniciadas com `_`).
- Use colchetes como `[Widget]` para criar links.
- O idioma da documentação deve ser inglês.

**Regras estritas:**

- **NÃO modifique a lógica do código.**
- **IGNORE completamente qualquer código que já esteja comentado com `//` ou `/* */`.**

Retorne o código completo do arquivo com a documentação atualizada.

eu preciso que vc modifique toda a documentacao em todas as partes necessarios, preciso da documentacao redonda, modifique. nao de nenhum comando de git.
